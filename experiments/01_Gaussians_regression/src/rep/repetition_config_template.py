import exputils as eu


##############################
# general config

config = eu.AttrDict(
	algorithm = "<algorithm>",
	n_neurons_per_input = <n_neurons_per_input>,
	is_trainable = <is_trainable>,
	ranges = <ranges>,
	m = <m>,
    name = "<name>",
    model = <model>,
    )
